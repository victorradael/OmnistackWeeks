import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); // Desenhando o App dentro do Body com id='root
//JSX (JavaScript + HTML)
